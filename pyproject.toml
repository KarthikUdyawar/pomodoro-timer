[project]
name = "pomodoro-timer"
version = "0.1.0"
description = "A simple and sweet Pomodoro timer built with Streamlit"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.55.0",
]
